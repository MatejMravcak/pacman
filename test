if (coordination_duch[1] == (HEIGTH) or coordination_duch[1] < 0):
    coordination_duch = -coordination_pacman

if (coordination_duch2[0] >= (WIDTH) or coordination_duch2[0] < 0):
    coordination_duch2 = -coordination_duch

if (coordination_duch2[1] >= (HEIGTH) or coordination_duch2[1] < 0):
    coordination_duch2 = -coordination_pacman

    if coordination_duch[0] == (WIDTH):
        platno.move(, -20, 0)

        if coordination_duch[0] < 0):
            platno.move(, 20, 0)






   if coordination_duch[0] == (WIDTH):
        platno.move(duch, -20, 0)
        okno.update()
    if coordination_duch[0] < 0:
        platno.move(duch, 20, 0)
        okno.update()
    if coordination_duch[0] == (HEIGHT):
        platno.move(duch, 0, -20)
        okno.update()
    if coordination_duch[0] < 0:
        platno.move(duch, 0, 20)
        okno.update()


    if coordination_duch2[0] == (WIDTH):
        platno.move(duch2, -20, 0)
        okno.update()
    if coordination_duch2[0] < 0:
        platno.move(duch2, 20, 0)
        okno.update()
    if coordination_duch2[0] == (HEIGHT):
        platno.move(duch2, 0, -20)
        okno.update()
    if coordination_duch2[0] < 0:
        platno.move(duch2, 0, 20)
        okno.update()
    for x in coordination_pacman:
        if coordination_pacman == coordination_duch:
            quit()






        if (duch.y + duch.height) > HEIGHT:
            duch.y = duch.y - 5
        if duch.y < 0:
            duch.y = duch.y + 5
        if duch.x < 0:
            duch.x = duch.x + 5
        if (duch.x + duch.width) > WIDTH:
            duch.x = duch.x - 5

        if (duch2.y + duch2.height) > HEIGHT:
            duch2.y = duch2.y - 5
        if duch2.y < 0:
            duch2.y = duch2.y + 5
        if duch2.x < 0:
            duch2.x = duch2.x + 5
        if (duch2.x + duch2.width) > WIDTH:
            duch2.x = duch2.x
